- Bocce bug: When going in the text area, the first keystroke is often lost (gains focus but do not act on the keystroke)

- When deleting a file/folder, if the currently selected one was deleted / close the view
- Also when deleting make sure the "nav" gets refreshed.

- help pane: allow to pick search kind: exact, startswith, contains


