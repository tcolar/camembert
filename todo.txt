- Bocce bug: When going in the text area, the first keystroke is often lost, especially if backspace/delete ?
   (gains focus but do not act on the keystroke)


BUG: Axon -> sometimes endup with duplicated functions ??
BUG: Axon space -> eval disspears .... nav buttons disappear

TrioInfo -> should be in Axon plugin

hide .git folder by default ... others ?

each plugin -> reindex option (remove reindex from status bar)
each plugin -> rescan for projects option
nd also indexspace pill -> refresh all projects (for all plugins)

FantomPodIndex ->   // TODO: key by path to allow multiple version of same pod ??

generic indexer or indexer per plugin ?
fix project finder fo axon plugin using new indexer
nav -> make collapse limit a setting
nav -> refresh, expand all, colapse all options ?
axonPlugin -> specify an env where to find documentation (pods)
axon -> pull/edit rukles
axon -> new faxon file -> validate/fix func name, requires lower case first letter !

always have new line at end of file on save

console: process tabs, repeat command etc ... ?
separate fantom config from editor config
make support fantom a plugin ?
instead of "gray" area when no file, open a "blank" file ?

splits screen: horizontal / vertical ? (own file browser or shared ??)
manyally -> open in side panel ??
ctrl + tab instead of ctrl + space

Goto in filespace/axon space -> list of files
goto -> use :25 instead of 25 for go to line ?

autosave option (when swicthing screen ?? )

better plugin speration

"pluginize" help pane -> allow search in html, javascript, java docs etc ...

make nav sidebar sash resizable ?

undo/redo at file level ?

themes / dark theme (~ subliminal)

conf /etc/camembert/1.1/
 - options.props
 - session.props
 - shortcuts.props
 - run.fog ? global or plugin folder ??
 - fantom/envs.props
 - tpl/axon.tpl
 - tpl/class.tpl
 - axon/license.props
 - theme/default .... dark ....


run -> terminate any existing process ??
or maybe have process list somewhere
separate console from search results ??


 ant projects, maven projects (graddle etc..), play ? and so on
 node support
 java support
 ruby, php, etc ...

  custom run/ commands build/whatever commands

  fantom/commands.fog:  $fan -> env/fan(.exe) automatically

  have a flag -> scope -> GLOBAL, SPACE (ie command only shows when corresponding space open or always)

  few vars:
  $env (current fantom, node whatever env)
  $dir -> project root dir
  $file -> current file if any
  etc ...

  "run", "f5", ["$fan"], interactive = true (can edit params)  -> memorize(mem/disk) or ask each time ?
                                  interactive -> NO, ALWAYS, ONCE, ONCE_PER_PROJECT ?, ONCE_PER_SESSION
  "build", "f4", ["$fan", "$dir/build.fan"], "$dir"
  "push", "f3", ["/bin/hg", "push"], "$dir"

  mercurial/git commands (pull -u, push, merge, status etc....)
  mercurial / git view ?


/etc/camembert/commands/run.json (default commands)
/etc/camembert/commands/fantom/run.json
/etc/camembert/commands/fantom/build.json ... custom commands (could be for space or global etc ...)
etc ...

file -> same
nav -> same .... but quick search / goto would need to be plugin specific or moved
process -> mve all that inside plugin  -> terminate : move to console area X
.... or keep them there but have them call out to plugin specific impls ???
panels -> same
options -> add "plugins" options : enable/disable plugins   remove swicth config(env)
help -> same
plugins -> remove
plugin[1] "Fantom" .. swicth env
plugin[1] "Axon" (End : fantom_env), import, licensing, about etc ...

# MultiNUmbered : srdDir.1 =; srdDir.2= etc... or just have it(srdDir=) mutiple times ???
# Multinamed : env.podDir.default = ;  env.podDir.skyspark = ;
# folder for themes
# folder for templates
# options.props, shortcuts.props
# run.fog -> replace with more generic props based
# axon_options.props or axon/options.props ?
# emty file template ????
# default template association config ? fan -> "fantom"  and so on (ordering ??)
# ordering by name 1_class.tpl, 2_mixin.tpl, 3_enum.tpl ?
# or have both global ones and plugins ones ??
# template/emptyfile.tpl   template/fantom/class.fan template/fantom/mixin.fan etc ...
# template/licences/default.tpl -> goes on top. when new file ask which license, memorize and reuse last pick by default.


